<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :UserName %><br />
    <%= f.text_field :UserName %>
  </div>
  <div class="field">
    <%= f.label :Password %><br />
    <%= f.password_field :Password %>
  </div>
  <div class="field">
    <%= f.label :Type %><br />
    <%= f.select :Type, options_for_select ( %w( root normal ) ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
